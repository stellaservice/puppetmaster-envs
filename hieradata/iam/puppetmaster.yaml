# iam_profile_name defaults, overrides apps/$::app_project but not nodes/$::trusted.certname
# Don't put any secrets here, this is a public repo!
---
classes:
  - bsl_core::derp

hello_worlds:
  - "environments/core/hieradata/iam/puppetmaster.yaml reporting in"

external_fqdn: 'puppetmaster.bitswarm.io'
